import PlaceholderStep from "./_PlaceholderStep";
import type { StepProps } from "@/types/intake";

const Step14 = (props: StepProps) => (
  <PlaceholderStep stepNumber={14} title={"Psychological Symptoms"} {...props} />
);

export default Step14;
