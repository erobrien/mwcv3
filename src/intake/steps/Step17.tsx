import PlaceholderStep from "./_PlaceholderStep";
import type { StepProps } from "@/types/intake";

const Step17 = (props: StepProps) => (
  <PlaceholderStep stepNumber={17} title={"Symptom Duration"} {...props} />
);

export default Step17;
