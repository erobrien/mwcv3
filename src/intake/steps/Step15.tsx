import PlaceholderStep from "./_PlaceholderStep";
import type { StepProps } from "@/types/intake";

const Step15 = (props: StepProps) => (
  <PlaceholderStep stepNumber={15} title={"Sexual Symptoms"} {...props} />
);

export default Step15;
