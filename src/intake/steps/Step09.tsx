import PlaceholderStep from "./_PlaceholderStep";
import type { StepProps } from "@/types/intake";

const Step09 = (props: StepProps) => (
  <PlaceholderStep stepNumber={9} title={"Medications"} {...props} />
);

export default Step09;
