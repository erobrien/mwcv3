import PlaceholderStep from "./_PlaceholderStep";
import type { StepProps } from "@/types/intake";

const Step13 = (props: StepProps) => (
  <PlaceholderStep stepNumber={13} title={"Physical Symptoms"} {...props} />
);

export default Step13;
