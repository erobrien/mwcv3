import PlaceholderStep from "./_PlaceholderStep";
import type { StepProps } from "@/types/intake";

const Step18 = (props: StepProps) => (
  <PlaceholderStep stepNumber={18} title={"Referral Source"} {...props} />
);

export default Step18;
