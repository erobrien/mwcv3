import PlaceholderStep from "./_PlaceholderStep";
import type { StepProps } from "@/types/intake";

const Step06 = (props: StepProps) => (
  <PlaceholderStep stepNumber={6} title={"Primary Care Provider"} {...props} />
);

export default Step06;
