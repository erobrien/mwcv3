import PlaceholderStep from "./_PlaceholderStep";
import type { StepProps } from "@/types/intake";

const Step19 = (props: StepProps) => (
  <PlaceholderStep stepNumber={19} title={"Consent & Signature"} {...props} />
);

export default Step19;
