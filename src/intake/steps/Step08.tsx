import PlaceholderStep from "./_PlaceholderStep";
import type { StepProps } from "@/types/intake";

const Step08 = (props: StepProps) => (
  <PlaceholderStep stepNumber={8} title={"Diagnosis Details"} {...props} />
);

export default Step08;
