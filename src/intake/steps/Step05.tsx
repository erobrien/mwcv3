import PlaceholderStep from "./_PlaceholderStep";
import type { StepProps } from "@/types/intake";

const Step05 = (props: StepProps) => (
  <PlaceholderStep stepNumber={5} title={"Emergency Contact"} {...props} />
);

export default Step05;
