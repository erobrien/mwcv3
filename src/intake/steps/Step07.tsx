import PlaceholderStep from "./_PlaceholderStep";
import type { StepProps } from "@/types/intake";

const Step07 = (props: StepProps) => (
  <PlaceholderStep stepNumber={7} title={"Diagnoses"} {...props} />
);

export default Step07;
