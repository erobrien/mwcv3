import PlaceholderStep from "./_PlaceholderStep";
import type { StepProps } from "@/types/intake";

const Step20 = (props: StepProps) => (
  <PlaceholderStep stepNumber={20} title={"Submitting…"} {...props} />
);

export default Step20;
