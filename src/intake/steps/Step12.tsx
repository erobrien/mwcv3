import PlaceholderStep from "./_PlaceholderStep";
import type { StepProps } from "@/types/intake";

const Step12 = (props: StepProps) => (
  <PlaceholderStep stepNumber={12} title={"Hormone Therapy"} {...props} />
);

export default Step12;
